server:
 port: 7078

resilience4j:
  retry:
    retry-aspect-order: 2
    instances:
      gatewayRetry:
        max-attempts: 3
        wait-duration: 5s
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2
        retry-exceptions:
         - org.springframework.web.client.RestClientException
        ignore-exceptions:
         - com.boa.resilience4jdemo.exceptions.IgnoreException
  circuitbreaker:
    circuit-breaker-aspect-order: 1
    instances:
      gatewayCircuitBreaker:
        wait-duration-in-open-state: 1m
        permitted-number-of-calls-in-half-open-state: 3
        sliding-window-type: count-based
        sliding-window-size: 5
        minimum-number-of-calls: 5
        slow-call-duration-threshold: 10s
        slow-call-rate-threshold: 60 
        failure-rate-threshold: 60

serviceUrl: http://localhost:8765/api/customer
alternativeServiceUrl: http://localhost:7074/individuals/v1.0/
serviceUrl1: http://localhost:8765/api/account/accounts/v1.0/
alternativeServiceUrl1: http://localhost:7076/accounts/v1.0/
